version: '3.8'

services:
  qwenproxy:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - proxy_storage:/app/browser_sessions
    restart: always

volumes:
  proxy_storage: